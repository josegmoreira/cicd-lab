name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  teste:
    runs-on: ubuntu-latest

    steps:
      - name: Baixar código
        uses: actions/checkout@v4

      - name: Mostrar mensagem
        run: echo "Pipeline CI executado com sucesso"
